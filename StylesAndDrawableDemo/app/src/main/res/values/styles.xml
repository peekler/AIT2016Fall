<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme" parent="AppTheme">
        <item name="android:textViewStyle">@style/ExampleStyle</item>
        <item name="android:buttonStyle">@style/ButtonStyled</item>
    </style>

    <style name="ExampleStyle">
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">#0000EE</item>
    </style>


    <style name="ButtonStyled" parent="Base.Widget.AppCompat.Button">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/button_bg</item>
        <item name="android:padding">10dp</item>
    </style>

</resources>
